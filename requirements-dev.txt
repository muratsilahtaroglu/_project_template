# Dev/test tooling only — NEVER installed into the production image (Dockerfile uses requirements.lock).
# Same pinning rules as requirements.txt: exact `==` only (rules.md §7).
# After editing, refresh the dev lock:
#   pip-compile --generate-hashes -o requirements-dev.lock requirements-dev.txt
#
# pytest==<exact.version>
# ruff==<exact.version>
# pip-audit==<exact.version>
# pip-tools==<exact.version>
# pre-commit==<exact.version>
