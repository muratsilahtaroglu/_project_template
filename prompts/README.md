# prompts/ — versioned LLM prompts

Reusable LLM prompts live here as **versioned files**; code reads them from here, never embeds prompt
strings. Naming: `<purpose>.v<N>.md`. Every prompt's role is recorded in `docs/architecture.md`.
